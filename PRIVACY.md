# 慎读隐私政策

生效日期：2026-09-07

慎读不收集、传输、出售或持久化保存个人数据。

扩展只在用户主动选择“显示中文拼音”后，使用浏览器授予的临时 `activeTab` 权限读取当前选区及其前后各最多 32 个字符。相关文本仅在当前页面和设备内用于计算拼音、定位浮层与调用系统语音；不会发送到服务器，也不会写入浏览器存储。

扩展不包含账号、分析、广告、遥测、远程代码或网络请求。关闭页面或导航后，按需注入的页面运行时及其内存状态随页面销毁。

朗读仅选择浏览器标识为本地服务的中文声音，不使用远程声音或默认语音回退。没有本地中文声音时不发起朗读。上下文不传给语音服务；朗读文本仅为浮层所显示的中文目标，最多八个汉字。

项目网页和 GitHub Issues 由 GitHub 运营，受其隐私政策约束；扩展不会自动访问这些网站。请勿在公开 Issues 中提交敏感选区或个人资料。

## English

Effective September 7, 2026. Shendu does not collect, transmit, sell, or persist personal data. Only after you invoke its context menu, the extension temporarily reads the selection and up to 32 surrounding Unicode characters on each side to compute pinyin locally and position its popover. Text is held in memory, not browser storage. The extension contains no accounts, ads, analytics, telemetry, remote executable code, or network requests.

Speech uses only Chinese voices identified by the browser as local services. No remote voice or default voice fallback is used. If no local Chinese voice is available, speech is unavailable. Only the displayed Chinese target (up to eight Han characters) is spoken, without surrounding context. Page navigation destroys the injected page runtime. GitHub hosts the project and support pages under its own privacy policy; the extension does not automatically access them. Do not post sensitive information in public issues.

## 繁體中文

生效日期：2026-09-07。慎讀不收集、傳輸、出售或持久保存個人資料。只有在您點選右鍵選單後，擴充功能才暫時讀取選取文字與前後各最多 32 個 Unicode 字元，在本機計算拼音及定位浮層。文字只保留於記憶體，不寫入瀏覽器儲存空間；無帳號、廣告、分析、遙測、遠端程式碼或網路請求。

朗讀僅使用瀏覽器標示為本機服務的中文聲音，不使用遠端或預設聲音作為後備。沒有本機中文聲音時不朗讀。語音只包含浮層顯示的中文目標（最多八個漢字），不包含上下文。網頁導覽會銷毀注入的執行環境。專案及支援頁面由 GitHub 提供並適用其隱私政策；擴充功能不會自動存取。請勿在公開 Issues 張貼敏感資料。

问题可通过项目仓库的 GitHub Issues 提交：https://github.com/YifuFei/shendu/issues
